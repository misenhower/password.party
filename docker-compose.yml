version: '2'

services:

  nginx:
    image: nginx
    restart: unless-stopped
    volumes:
      - ./docker/nginx/conf.d:/etc/nginx/conf.d:ro
      - ./logs:/logs
      - ./public:/usr/share/nginx/html:ro
